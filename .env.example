# Docker Compose configuration — copy to .env and fill in real values.
# Both values are required; compose refuses to start without them.

DB_PASSWORD=change_me
API_KEY=change_me

# Optional overrides
#DB_NAME=onecloudsync
#DB_USER=postgres
#CORS_ORIGIN=http://localhost:8080
